defmodule RegentUi do
  @moduledoc """
  Shared Regent UI package entrypoint.

  This package ships the `Regent.*` Phoenix components (primitives, ruled-sheet
  structure, blog, profile, theme toggle, holographic card and agent metadata),
  their CSS, the packaged Geist fonts and the small profile, blog and holographic
  card browser modules.
  """
end
